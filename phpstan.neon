parameters:
    level: 8

    paths:
        - lib
        - bundle

    ignoreErrors:
        # PHPStan

        - '#extends generic class Symfony\\Component\\Form\\\w+ but does not specify its types: TData#'
