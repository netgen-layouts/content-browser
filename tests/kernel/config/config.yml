imports:
    - { resource: security.yml }

framework:
    secret: secret
    router:
        resource: '%kernel.project_dir%/config/routing.yml'
        strict_requirements: true
    translator: ~
    test: ~
