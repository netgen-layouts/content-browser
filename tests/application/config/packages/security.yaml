security:
    providers:
        in_memory:
            memory:
                users:
                    admin:
                        password: admin

    encoders:
        Symfony\Component\Security\Core\User\User: plaintext

    firewalls:
        main:
            http_basic: ~
